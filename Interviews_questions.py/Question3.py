# What is the enumerate() function in Python?
# answer
# enumerate() iterates through a sequence and extracts the index position and 
# its corresponding value too.

for i,v in enumerate(['python','C++']):
    print(i,v)